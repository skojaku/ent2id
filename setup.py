#!/usr/bin/env python
# encoding: utf-8
from setuptools import setup, find_packages

setup(name='ent2id', 
      version='0.0.1', 
      description = 'A lightweight toolbox for entity recognition', 
      author = 'Sadamori Kojaku', 
      packages=find_packages(),
      zip_safe=False)
